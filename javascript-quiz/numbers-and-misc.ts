/**
 * Question1:
 */
let number = 0;
console.log(number++); // 0
console.log(++number); // 2
console.log(number); // 2

const a: number = 3;
const b = new Number(3);

/**
 * 255..toString() -> This is correct syntax.
 * The reason that this is correct is because the first dot is a decimal point, and the second dot is a property accessor.
 */

/**
 * Question2:
 * 1. We can add functions as keys in map.
 */
